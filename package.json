{
  "name": "entities-map",
  "version": "1.0.0",
  "description": "inventaire entities map in HTML/CSS",
  "main": "index.js",
  "scripts": {
    "start": "live-server . -w index.html main.*",
    "watch": "while true; do inotifywait -r ./src -e modify && ./scripts/build; done",
    "build": "./scripts/build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "maxlath",
  "license": "MIT",
  "devDependencies": {
    "live-server": "~1.1.0",
    "node-sass": "~3.10.0"
  }
}
